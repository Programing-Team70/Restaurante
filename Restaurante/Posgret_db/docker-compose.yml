services:
  postgres:
    image: postgres:13
    container_name: restaurant_postgres
    environment:
      POSTGRES_USER: Restaurant
      POSTGRES_PASSWORD: ¡R8ta!
      POSTGRES_DB: restaurant_users
    ports:
      - "5436:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data: